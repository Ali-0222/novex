# Novex Pest Control

Next.js website for `novexpestcontrol.com.au`.

## Setup

```bash
npm install
npm run dev
```

## SMTP

Copy `.env.example` to `.env.local` and set the SMTP values before using the contact form in production.
